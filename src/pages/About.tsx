import React from "react";
import { Box, Typography } from "@mui/material";

const AboutPage: React.FC = () => (
  <Box>
    <Typography variant="h4">О проекте</Typography>
    <Typography>Это информация о проекте.</Typography>
  </Box>
);

export default AboutPage;
